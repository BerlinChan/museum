<!DOCTYPE html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%- (page.title ? page.title + ' | ' : '') + config.title %></title>

    <%- open_graph({
        title: (page.title ? page.title + ' | ' : '') + config.title,
        image: page.screenShot,
        twitter_card: 'summary_large_image',
        twitter_id: '@BerlinChanCom',
        twitter_site: '@BerlinChanCom'
    }) %>

    <link rel="apple-touch-icon" sizes="180x180" href="<%= config.root %>apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="<%= config.root %>favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="<%= config.root %>favicon-16x16.png">
    <link rel="manifest" href="<%= config.root %>site.webmanifest">
    <link rel="mask-icon" href="<%= config.root %>safari-pinned-tab.svg" color="#5bbad5">
    <meta name="msapplication-TileColor" content="#ffe98a">
    <meta name="theme-color" content="#ffe98a">

    <!-- framework CSS -->
    <link href="//cdnjs.cloudflare.com/ajax/libs/bulma/0.7.5/css/bulma.min.css" rel="stylesheet">
    <link href="//cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
    <!-- Custom styles for this template -->
    <%- css('css/index.css') %>
</head>
